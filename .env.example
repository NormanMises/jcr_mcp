# JCR MCP服务器环境变量配置示例
# 复制此文件为 .env 并修改配置

# 传输协议
# 可选值: stdio, sse, streamable-http
# - stdio: 标准输入输出，用于本地客户端（如Claude Desktop）
# - sse: Server-Sent Events，用于Web客户端和远程部署
# - streamable-http: HTTP流式传输，用于远程部署
JCR_MCP_TRANSPORT=stdio

# 监听地址（仅在 sse 或 streamable-http 模式下使用）
# 0.0.0.0 表示监听所有网络接口
JCR_MCP_HOST=0.0.0.0

# 监听端口（仅在 sse 或 streamable-http 模式下使用）
JCR_MCP_PORT=8080
